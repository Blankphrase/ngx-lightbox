import { OnDestroy, OnInit } from '@angular/core';
import { NgxHotkeysService } from '../ngx-hotkeys.service';
import { IHotkey } from '../interfaces';
export declare class NgxCheatsheetComponent implements OnInit, OnDestroy {
    private hotkeysService;
    title: string;
    helpVisible: boolean;
    hotkeys: IHotkey[];
    private _subscription;
    private _map;
    constructor(hotkeysService: NgxHotkeysService);
    ngOnInit(): void;
    formatHotkey(hotkey: IHotkey): string[];
    private symbolize(combo);
    ngOnDestroy(): void;
    toggleCheatSheet(): void;
}
